# Daily Memory: 2026-05-13

## 早上排查：昨晚 cow 未成功重启
- YQ 昨晚发"在吗"无回复，今早 `cow status` 显示未启动，`cow start` 后才恢复
- 排查结论：大概率是昨天修改 scheduler_service.py（时区归一化）后重启，.pyc 缓存不一致导致启动崩溃；也可能是 cow restart 的 start 阶段失败
- 日志里昨晚完全无记录，进程处于静默死亡状态

## 看门狗托管重启方案设计
- 讨论了"Agent 离线自更新后可靠重启+通知"的方案
- 架构：Agent 写 pending_restart 标记 + pending_notify.json → Windows 任务计划程序每 5min 托管执行 cow stop → 清 .pyc → cow start → 确认存活 → 新进程读 pending_notify.json 发通知
- 已录入 FEATURES.md 和 MEMORY.md 的待开发功能，标记为 📐 已设计
## ref-downloader skill 安装与配置（15:46-16:00）
- 从 GitHub ltczding-gif/ref-downloader 下载并安装到 skills/ref-downloader/
- pip 安装 playwright + pymupdf 依赖
- YQ 翻墙安装了 playwright chromium 浏览器驱动（playwright install chromium）
- 创建 config.local.toml，配置邮箱 yan_qi@buaa.edu.cn
- 修复 4 个脚本的 sys.path 导入问题（找不到 _config 模块）
- 将 channel="msedge" 改为 channel="chromium"（系统 Edge 不在 Playwright 预期路径）
- REF_DOWNLOADER_EDGE_PROFILE 指向 Chrome 用户数据目录
- 端到端测试通过：DOI 10.1038/s41586-020-2649-2 → 提取 57 条引用 → 验证 35 条有 DOI → 下载 12 个 PDF
