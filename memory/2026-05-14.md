## 看门狗与重启测试

**看门狗全链路验证通过 ✅**（2026-05-14 约 10:30-11:00）
- 问题1: watchdog.bat 的 `>> nohup.out 2>&1` 与 `cow start` 内部重定向冲突 → 爆 `Permission denied: nohup.out`
- 修复: 改为 `--no-logs` 参数，去掉 shell 层重定向
- 问题2: 延迟通知走 scheduler 发不出去（weixin 无 context_token）
- 修复: 改为 `.delayed_notify.txt` 机制，app.py 启动时写入，等用户发消息拿到 token 后发送
- 问题3: `_check_pending_notification.txt` 路径写错为 `notifications`（漏了 s）
- 重要经验: `cow start` 前必须先关旧的 cmd 窗口
- 看门狗用完后 `schtasks /Delete /TN \CowAgentWatchdog /F` 清理
## 看门狗自毁修复（20:52）
- 问题：`\CowAgentWatchdog` 计划任务为一次性 + 每3分钟重复∞ → 每次触发弹 cmd 窗口
- 修复：watchdog.bat 末尾增加 `schtasks /Delete` 自毁逻辑
- 操作：删除了 `\CowAgentWatchdog` 任务（`schtasks /Delete /TN "\CowAgentWatchdog" /F`）
- FEATURES.md #3 笔记更新为「已修复」
- MEMORY.md 重启流程增加第5条提醒

## 延迟通知测试（20:xx）
- 延迟通知链路测试：cow stop → 看门狗重启成功 → 微信通知送达
- 修复：`check_pending.py` 和 `app.py` 中 `.delayed_notify.txt` 路径统一指向 `scripts/.delayed_notify.txt`

## Daily Summary (23:55)

- 调度器提醒 bug 排查：发现 once 型提醒未触发，可能与不带时区的 datetime 问题有关，已记录至 FEATURES.md，并人工补发了 19:00 的提醒；随后设置了 20:00 喝水测试提醒，
